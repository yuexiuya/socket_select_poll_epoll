INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
FILE(GLOB SRC_LIST "${CMAKE_CURRENT_SOURCE_DIR}/*.h"
                "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp"
    )

ADD_EXECUTABLE(epoll_socket ${SRC_LIST})
TARGET_LINK_LIBRARIES(epoll_socket pthread)
